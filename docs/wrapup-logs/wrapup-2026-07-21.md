# wrapup 2026-07-21

## 改动概览

1. **单 exe 架构**：工具箱功能合入 `server/index.js`，一个 exe 启全部端口
2. **控制面板重构**：三个独立按钮（游戏/抖音/B站）+ 日志过滤 + 状态同步
3. **B站 relay Node.js 重写**：`server/danmaku/bilibili.js`，消除 Python 依赖
4. **日志系统强化**：`logger.onLog` → 所有模块日志进面板环缓冲
5. **适配器启停控制**：去 `process.exit()`，导出 start/stop/isRunning
6. **Tauri v2**：编译通过，缺 WebView2，保留代码暂停

## 错误复盘

| 错误 | 原因 | 流程问题? | 已写 pitfalls? |
|------|------|:--:|:--:|
| WS 每 5s 断连循环 | app.js 三个 setTimeout(connectWS) 冲突 | 否 | 否 |
| 旧进程反复残留 | 没有可靠杀进程机制 | 是 | ✅ G16 |
| "全部停止"不完整 | 只停适配器没停引擎 | 否 | 否 |
| Tauri 窗口空白 | cargo build 不打包前端 | 否 | 新增 |
| 面板无弹幕日志 | logger 输出没进 LOG_RING | 否 | 否 |
| 弹幕链路不通 | /api/start-douyin 只 spawn 没 start | 否 | 否 |
| B站刷屏 | 重连无限制 | 否 | 否 |
| 按钮"启动游戏"自动带弹幕 | 用户说反了，我加了 auto-start | 是 | 否 |

## 资产变更

- 更新 `.claude/rules/pitfalls.md`（G16: 旧进程残留）
- 覆写 `HANDOVER.md`
- 追加 `PROGRESS.md`（2026-07-21 条目）
- 删 `server/danmaku/douyin-live-test.js`（调试残留）
- 清理过期 plan 文件

## Top 5 待办

1. 更新 B站 Cookie，验证弹幕全链路
2. `node build.js` → dist exe → 端到端测试
3. 清理废弃文件: toolbox/server.js, bilibili-relay.py
4. 直播实测全流程
5. Tauri: 装 WebView2 或放弃
